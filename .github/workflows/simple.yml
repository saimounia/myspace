name: shell commands

on: [push]

jobs:
  run-shell-command: 
    runs-on: ubuntu-latest
    steps:
      - name: echo a string 
        run: echo "HEllo world"
      - name: echo a string 
        run: |
          node -v
          npm -v
