name: shell commands

on: [push]

jobs:
  run-github-actions:
    runs-on: ubuntu-latest
    steps:
      - name: Js Action
        uses: actions/hello-world-javascript-action@v1.1
        with: 
          who-to-greet: Mounika
          


